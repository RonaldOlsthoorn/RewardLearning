function [max_outcome, set,  max_epd] = find_max_outcome(S_original, S, rm, ro_par)

%Calls wrapper function. Convenient for imports.
[max_outcome, set,  max_epd] = find_max_outcome(S_original, S, rm, ro_par);

end